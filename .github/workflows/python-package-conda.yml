name: Python Package using Conda

# Disabled for Vercel/Next.js frontend deployment.
# This repository is currently being used as a voice-chat web app on Vercel.
# The old Python/Conda workflow was running on every push and failing, but it is not required for the frontend launch.
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow disabled
        run: echo "Python Conda workflow is disabled for frontend deployment."
